"use client"
import {
  Popover as SimplifiedPopover,
  PopoverTrigger as SimplifiedPopoverTrigger,
  PopoverContent as SimplifiedPopoverContent,
} from "./simplified-popover"

const Popover = SimplifiedPopover
const PopoverTrigger = SimplifiedPopoverTrigger
const PopoverContent = SimplifiedPopoverContent

export { Popover, PopoverTrigger, PopoverContent }
